# forecasting/models.py
#
# Моделей у приложения нет — и это намеренно.
#
# Здесь жила CounterpartyCategory: категория контрагента по объёму продаж.
# Таблица простояла пустой всё время существования и не читалась ни одной
# строкой кода. Категории считает CounterpartyCategorizator
# (services/categorization/categorizer.py) — на лету, в памяти, не заглядывая
# в базу. Удалена 26 августа 2026, миграция 0002_delete_counterpartycategory.
#
# Само приложение остаётся: в services/ лежат ABC-анализ, сезонность
# и оптимизатор закупки, которыми пользуются api/views/.
